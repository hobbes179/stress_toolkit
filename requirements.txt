# Stress Toolkit — runtime dependencies
# Pinned to versions verified locally; re-verify on Streamlit Cloud after any bump.
# Floor is 1.49: that is where `width="stretch"` / `width="content"` landed on
# st.dataframe and st.pyplot (1.46 covered most other elements). The app uses
# the `width=` form exclusively -- `use_container_width` is deprecated and is
# already being removed element-by-element upstream. Capped below 2 so a major
# release cannot change the runtime under a deployed app without a review.
streamlit>=1.49,<2
numpy>=1.24.0
pandas>=1.5.0
matplotlib>=3.6.0
plotly>=5.18            # interactive stress contour (Phase 6); ships with streamlit

# v2 — FEM solver (Phase 4). sectionproperties pulls in shapely + a meshing
# backend (cytriangle) + scipy. Verify these install on Streamlit Cloud
# (deploy a throwaway branch) before relying on the FEM path in production.
sectionproperties>=3.7,<4
shapely>=2.0

# v2 — custom-section import (Phase 5). DXF parsing.
ezdxf>=1.3

# Tie-rod module — L-BFGS-B in the layout search (library/tierod/optimize.py).
# Listed explicitly because it is now a DIRECT dependency: it arrived in the
# tree only as a transitive of sectionproperties, which would make the layout
# search break on an unrelated dependency change.
scipy>=1.11
